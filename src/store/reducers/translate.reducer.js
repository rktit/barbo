// import * as Actions from "../actions";

const initialState = {
		data: [],
};

const translate = function (state = initialState, action) {
		switch (action.type) {
				default:
						return {
								...state,
						};
		}
};

export default translate;